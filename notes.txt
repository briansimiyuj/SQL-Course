Unique constraint enable us to ensure that no duplicate values are entered in a column.

If we want to add a unique constraint to a column, we can use the following:
    `CREATE TABLE $table_name(

        $column_name $data_type UNIQUE, 

    )`