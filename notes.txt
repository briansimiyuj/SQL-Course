Check constraint is used to enforce a condition on a column or a set of columns in a table. We can use:
    `CREATE TABLE $table_name(
        $column_name $data_type CONSTRAINT $constraint_name CHECK ($condition)
    )`