Not null constraint enables us to prevent null values in the column. To add a not null constraint, we use:
    `ALTER TABLE $table_name
    MODIFY $column_name $data_type NOT NULL;`

To create a table with a not null constraint, we use:
    `CREATE TABLE $table_name (
        $column_name $data_type NOT NULL,
        ...
    );`